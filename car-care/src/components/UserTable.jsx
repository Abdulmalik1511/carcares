import React from 'react'

export default function UserTable() {
  return (
    <div>UserTable</div>
  )
}

// Displays registered users:

// View user name, email, number of bookings

// Option to deactivate/remove users (if needed)