import React from 'react'

export default function BookingList() {
  return (
    <div>BookingList</div>
  )
}


// Shows all incoming service booking requests:

// Filter by status (Pending, Approved, Completed)

// Admin can approve, assign, or cancel bookings